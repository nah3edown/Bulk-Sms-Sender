services:
  - type: worker
    name: telegram-sms-bot
    env: python

    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py

    envVars:
      - key: BOT_TOKEN
        sync: false

      - key: SMS_API_KEY
        sync: false

      - key: ADMIN_IDS
        sync: false
